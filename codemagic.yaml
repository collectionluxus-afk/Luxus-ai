workflows:
  android-debug:
    name: Luxus AI Android APK
    max_build_duration: 30
    environment:
      java: 17

    scripts:
      - name: Build debug APK
        script: |
          gradle assembleDebug --no-daemon

    artifacts:
      - app/build/outputs/apk/debug/*.apk
